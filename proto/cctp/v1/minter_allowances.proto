syntax = "proto3";

package cctp.v1;

option go_package = "github.com/strangelove-ventures/noble/x/cctp/types";

// The minter allowances in the CCTP module apply to all mints
// Amount refers to uunits, like uusdc.
message MinterAllowances {
  string denom = 1;
  uint64 amount = 2;
}
